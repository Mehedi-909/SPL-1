#include<stdio.h>
#include<string.h>
int main() {
    int num1 ,num2 ,sum = 0,i = 0,total = 0;
    scanf("%d %d",& num1 , & num2 );
    sum = num1 + num2;
    printf("Sum is : %d \n", sum );
    for( i = 0 ; i < 5 ; i++ ) {

	 }
    for(i = 1; i <= num1 ;i++){
        total = total + i ;

	 }
    printf("Total is: %d\n", total );
    printf("Number1 is %d: ", num1 );
}